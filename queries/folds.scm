; Foldable regions: type bodies and the blocks inside them.

(simple) @fold
(module) @fold
(network) @fold
(channel) @fold
(channel_interface) @fold
(moduleinterface) @fold

(parameters) @fold
(gates) @fold
(types) @fold
(submodules) @fold
(connections) @fold

(submodule) @fold
(connection_group) @fold
(property_decl) @fold

; XML / object and array literals
(object) @fold
(array) @fold

; Consecutive line comments
(comment) @fold
